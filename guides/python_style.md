# Python Style Guide

## 1. Formatting
- Use `ruff` for formatting.
- Line length: 100 characters.

## 2. Type Hints
- Use strict type hints.
- Use `mypy` or `ruff` for type checking.

## 3. Docstrings
- Use Google style docstrings.
- Document all functions and classes.
